package dev.limucc.glinteverything.client.glint;

/** Duck interface mixed onto {@code ItemStackRenderState} and {@code ItemSubmit} to carry the tint. */
public interface GlintTinted {
    int ge$glintColor();
    void ge$glintColor(int color);
}
